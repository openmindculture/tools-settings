# Emulate CSS Media Type Print in Firefox Browser Dev Tools and in Chrome

- in Chrome/Chromium: drawer Rendering tab: emulate CSS media type,
- in Firefox in style details near `:hov :cls dark light:`

## 2. The Panel Layout
Look at the right half of your DevTools window, which displays your CSS rules (the Rules sub-tab).

## 3. The Target Sub-Toolbar
Directly above the element { } styling selector, there is a dedicated internal toolbar.

## 4. The Toggle Icon
The Toggle print media simulation icon is the leftmost button on that specific CSS Rules toolbar. It looks like a tiny sheet of paper with text on it or a miniature printer.
